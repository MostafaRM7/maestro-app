{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the primary Maestro window",
  "windows": ["main", "project-*"],
  "permissions": ["core:default"]
}
