[package]
name = "maestro-redaction"
description = "Deterministic secret redaction for Maestro logs, events, errors, and exports"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
license = "MIT"
repository = "https://github.com/maestro-app/maestro-app"
authors = ["Maestro contributors"]
# This manifest declares package fields directly rather than through workspace
# inheritance; `publish` itself is inherited per-field from [workspace.package].
publish.workspace = true

[dependencies]
serde_json.workspace = true

[lints]
workspace = true
